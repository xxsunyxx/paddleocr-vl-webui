# PaddleOCR-VL 镜像配置
API_IMAGE_TAG_SUFFIX=latest-offline
VLM_BACKEND=vllm
VLM_IMAGE_TAG_SUFFIX=latest-offline

# 前端服务配置（在 docker-compose.yml 中已配置）
# PADDLE_SERVICE_URL=http://paddleocr-vl-api:8080/layout-parsing

